name: Publish FinFolio Web App

on:
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v2
      with:
        dotnet-version: 6.0.x
    - name: Restore dependencies
      run: dotnet restore
   # - name: Build-Web-App
     # run: dotnet build FinFolio.Web/FinFolio.Web.csproj --configuration Release --no-restore
    - name: Publish Web App
      run: dir #dotnet publish FinFolio.Web/FinFolio.Web.csproj --configuration Release --no-restore -o ../FinFolioWebApp -r linux-x64
   
